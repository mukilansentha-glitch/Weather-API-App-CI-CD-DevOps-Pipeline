[tool:pytest]
testpaths = tests
addopts =
    --cov=app
    --cov-report=xml:coverage.xml
    --cov-report=html:htmlcov
    --cov-report=term-missing
    --cov-fail-under=80

[flake8]
max-line-length = 120
exclude = .venv,__pycache__,htmlcov,.scannerwork
